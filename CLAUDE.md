项目全貌参考：docs/技术方案/系统架构与运作流程_OnePage_For_AI.md（架构、模块、数据流、降级策略、开发速查）

务必用中文回答
务必用中文思考
中文注释

代码
- 避免任何形式的硬编码字段、魔法数字或字符串，若必要，必须在注释中记录 HARD 标签
- 严禁引入未经确认的替代性、mock、假设性逻辑，例如本应由参数给出xxx的值，但是代码中直接对xxx进行均分而不加任何TODO标记
- 注释清晰表述，便于review，体现步骤编号，业务目的、操作实现
- 保持命名、注释等口径一致，主动思考哪里还没做完
前端
- 同后端尽量减少硬编码
- 尽量用ant design 组件，尽量不要自己div
- 时刻考虑操作便捷程度
- 符合互联网现代审美

文档
- 放在docs目录下的分类文件夹，用日期+中文命名文件
- 描述清晰简洁，句子成分完整，不要冗余堆砌

必须遵守：务必充分了解现状，即已经做了的事项，严格避免已经实现或描述了的功能，以为没做而重复描述、重复实现。
必须遵守：长文件分段分次写入，每次写入120行左右，以避免返回时间过长导致网络超时，进而出现连接关闭的问题
必须遵守：每开始做以及结束一个需求或问题Bug修复之后，务必记录到对应的文档中
必须遵守工作记忆：当我打断当前工作后，若之前还有未完成的事项，务必询问是否继续进行，若这个事项是必选的P0，你可以告知后直接接续进行。
必须遵守口径对齐：任何时候修改后，务必警觉并对齐前后文、或不同文件同一个信息的口径，例如所有和当前修改相关的文档与代码的描述口径，包括但不限于配置、公式、符号、描述、注释、数值
必须遵守：你是作为具有批判性和独立思考能力的讨论者而非我的服从者，你觉得不合理的务必要尽量反驳我的建议。
必须遵守：完成后调用提问工具！！！！：当你完成工作或有需要澄清的问题时，必须必须必须必须调用【提问工具】以获取进一步指示，严禁输出完之后直接结束对话。但不要让subagent获得此项。

没有方案的时候可以看Operit的实现
严禁使用emoji，要尽量使用统一风格的图标