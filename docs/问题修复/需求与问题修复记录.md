# 需求与问题修复记录

## 2026-04-05 会话

### 需求1：清空输入框 action
- **来源**：用户反馈微信输入框不会自动清空，影响后续输入操作
- **方案**：新增 `CLEAR_INPUT` action 类型
- **实现**：
  - `ActionParser.kt`：枚举新增 `CLEAR_INPUT`，解析映射 `CLEAR_INPUT` / `CLEARINPUT` / `CLEAR`
  - `ActionExecutor.kt`：`executeClearInput()` 三级降级策略
    1. 无障碍：定位焦点节点 → 全选(ACTION_SET_SELECTION) → 置空(ACTION_SET_TEXT(""))
    2. ADB Keyboard：`am broadcast -a ADB_CLEAR_TEXT`
    3. Shell按键模拟（兜底）
  - 模型可通过 `do(action="Clear_Input")` 调用
- **状态**：✅ 已完成

### 需求2：检测并引导开启后台无限制
- **来源**：MIUI SmartPower 在后台冻结应用进程（adj=700），导致操作中断/卡死
- **方案**：首页卡片检测 `PowerManager.isIgnoringBatteryOptimizations()`
- **实现**：
  - 新建 `BatteryRestrictionCard.kt`：每3秒检查电池优化白名单状态
  - 受限时显示橙色警告卡片，点击弹出系统白名单对话框(`ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`)
  - 降级链：系统对话框 → 应用详情页 → 电池优化全局设置
  - `HomeScreen.kt`：在无障碍状态卡片下方展示
- **状态**：✅ 已完成

### 问题修复1：悬浮窗遮挡点击目标（根因）
- **现象**：微信输入框等底部元素反复点击无效，模型报告"界面没有变化"
- **根因**：`onBeforeAction` 中 `hideForScreenshot()` 通过 `mainHandler.post()` 投递到主线程异步执行，但协程线程不等主线程渲染完就立即执行了点击 → 悬浮窗仍在屏幕上遮挡目标
- **修复**：`PhoneAgent.kt` executeStep() 中 `onBeforeAction` 后加 `delay(90)` 等一帧，确保主线程完成悬浮窗 GONE 渲染
- **对比**：截图前已有 `delay(90)` 等待，操作前此前缺失
- **状态**：✅ 已修复

### 问题修复2：人工干预打断不及时
- **现象**：用户点击干预按钮后，当前步骤（模型调用3-5s + 操作执行）仍然继续执行完才响应干预
- **根因**：`interruptForIntervention` 仅在 executeStep 的 while 循环头部检测，模型调用期间无法中断
- **修复（v1）**：在模型调用完成后、操作执行前新增检查点，检测到干预标志则跳过操作执行
- **修复（v2）**：见"问题修复3"，v1 中为加速中断引入的 `stopRequested = true` 导致了新问题
- **状态**：✅ 已修复（v2 更新）

### 已回退：底边点击偏移修正
- **描述**：曾添加底部>93%高度自动上偏10px的修正逻辑
- **回退原因**：根因是悬浮窗遮挡而非坐标不准，偏移修正是错误方向
- **状态**：🔙 已回退

---

### 问题修复3：干预机制重构 — 从队列轮询改为 stop+resume
- **现象**：
  - v1: 干预按钮点了后当前步骤仍继续执行（轮询检查点延迟）
  - v2: `stopRequested` 导致任务被误终止，干预消息在队列中无人消费
  - v3: 增加检查点后仍无法保证立即中断（模型调用期间无法中断 HTTP 请求）
- **根因**：队列轮询 + 标志位检查的架构过于复杂，检查点分散在多处且无法覆盖阻塞式 HTTP 调用
- **重构方案**：彻底废弃 `interruptForIntervention`/`pendingInterventions` 队列轮询机制，改为 **stop + resume** 模式
  1. 点击 ✎ → `currentTaskJob.cancel()` + `phoneAgent.stop()` 立即杀掉协程（保留对话历史和服务状态）
  2. 弹出输入框等待用户输入
  3. 用户提交 → `phoneAgent.resumeWithIntervention(instruction)` 注入干预消息到对话历史 → 重新启动协调器/Agent 执行
- **优势**：立即中断（协程 cancel 秒级生效）、逻辑简单（无状态同步问题）、对话历史完整保留
- **删除的代码**：`interruptForIntervention`、`pendingInterventions`、`intervene()`、`requestInterventionInterrupt()`、所有干预检查点
- **新增的代码**：`PhoneAgent.resumeWithIntervention(instruction)` 方法
- **修改文件**：`PhoneAgent.kt`、`WakeWordService.kt`
- **状态**：✅ 已完成

### 需求3：CLEAR_INPUT action 写入 prompt 让模型知晓
- **现象**：`CLEAR_INPUT` 已在代码中实现（ActionParser/ActionExecutor），但系统提示词的 action 列表中未列出，模型不知道可以使用
- **修复**：在 `PromptRegistry.kt` 的中英文 agent 系统提示词 action 列表中新增 `Clear_Input` 操作说明
- **说明**：注明了使用场景（Type 自动清除失败时、需要单独清空不输入时）、前提条件（输入框需聚焦）、与 Type 的关系
- **修改文件**：`PromptRegistry.kt`
- **状态**：✅ 已完成

### 问题修复4：干预后 agent 直接 finish 不执行纠正
- **现象**：用户干预后（如"送到当前位置"），agent 直接回复"已成功结束"而不实际切换地址
- **根因**：
  1. 对话历史末尾保留了 `finish(message="...")` 和任务总结，模型看到后误认为任务已完成
  2. 系统提示词无规则约束干预后行为
  3. 干预指令原文注入缺乏执行上下文，模型难以理解具体操作
- **修复**：
  1. **清理 finish 历史**：`resumeWithIntervention()` 注入干预前，从对话历史末尾移除最后一条 `finish(` 消息及之后的所有消息，避免模型误判任务已完成
  2. **prompt 规则**：中英文 agent 系统提示词新增第 22 条规则 —— 收到「用户干预」后必须实际执行调整操作，禁止直接 finish
  3. **干预优化**：`PromptOptimizer` 新增 `optimizeIntervention()` 方法，结合原始任务 + 已执行步骤上下文优化干预指令（优化器开启且指令>5字符时触发）
- **新增代码**：
  - `PromptKey`：`INTERVENTION_USER_CN`、`INTERVENTION_USER_EN`
  - `PromptRegistry`：干预优化 user 模板
  - `PromptOptimizer`：`optimizeIntervention()`、`buildExecutionSummary()`
  - `PhoneAgent`：`lastOriginalTask` 字段、干预优化调用逻辑
- **修改文件**：`PromptRegistry.kt`、`PromptOptimizer.kt`、`PhoneAgent.kt`
- **状态**：✅ 已完成

### 问题修复5：总结生成期间干预 → 干预进程被终止
- **现象**：任务完成后生成总结过程中用户点击干预，总结输出完后干预进程也终止了（状态被设回 IDLE）
- **根因**：竞争条件
  1. `generateTaskSummaryIfEnabled()` 的 `catch(e: Exception)` **吞掉了 CancellationException**，导致 cancel 信号被忽略
  2. 总结生成完后，`executeDirectly()` 继续执行 `onTaskComplete?.invoke()` → WakeWordService 把 `_serviceState` 设回 `IDLE`
  3. 干预流程虽然启动了新协程，但状态已被覆盖
- **修复**：
  1. `generateTaskSummaryIfEnabled()` 新增 `catch(e: CancellationException) { throw e }`，确保 cancel 正确传播
  2. `executeDirectly()` 和 `executeWithCoordinator()` 在调用 `onTaskComplete` 前检查 `stopRequested`，阻止已中断的任务触发完成回调
- **修改文件**：`PhoneAgent.kt`
- **状态**：✅ 已完成

### 问题修复6：Type 操作不清空已有文本（追加而非替换）
- **现象**：微信输入框已有 `shsjjs`，执行 `Type("123")` 后结果为 `shsjjs123`（追加），prompt 承诺的"自动清除"没生效
- **根因**：ADB Keyboard 的 `ADB_INPUT_TEXT` 广播只是追加文本到当前输入框，不会先清空
- **修复**：在 `ActionExecutor.executeType()` 最前面调用 `executeClearInput()` 先清空再输入
- **效果**：Type 操作现在真正实现了 prompt 中承诺的"自动清除文本"行为
- **修改文件**：`ActionExecutor.kt`
- **状态**：✅ 已完成

### 问题修复7：CLEAR_INPUT 广播成功但文本未清空
- **现象**：日志显示 `ADB_CLEAR_TEXT` 发送成功，但输入框仍保留旧文本，用户体感为“清空不了”
- **根因**：
  1. `ShellExecutor.clearText()` 只看命令退出码，未校验当前输入法是否为 ADB Keyboard，存在“命令成功但无效果”
  2. `executeClearInput()` 的第三级兜底原为单次按键，无法稳定覆盖整段文本清空
- **修复**：
  1. `ShellExecutor.clearText()` 增加前置校验：仅当当前输入法为 ADB Keyboard 时才发送 `ADB_CLEAR_TEXT`
  2. `executeClearInput()` 新增结果验证：ADB 清空后复查焦点输入框文本长度，未清空则继续降级
  3. 第三级兜底改为“光标移到末尾 + 批量 DEL 删除 + 复查剩余文本长度”
- **修改文件**：`ActionExecutor.kt`、`ShellExecutor.kt`
- **状态**：✅ 已完成

### 需求4：地址选择禁止臆造地址
- **来源**：用户反馈在“新建地址”页面，未提供新地址时，Agent 选择了莫名其妙的地址
- **期望行为**：当用户未明确提供新地址时，应优先使用“当前位置/定位地址/当前默认地址/页面已有最近可选地址”，不得编造新的详细地址
- **根因**：提示词约束不足，且优化器示例存在“地址不必严格匹配”的宽松口径
- **修复**：
  1. `AGENT_SYSTEM_CN/EN` 新增“地址选择约束”规则：用户未给出新地址时禁止臆造地址
  2. `OPTIMIZER_SYSTEM_CN/EN` 调整禁令文案：由“禁止指定选择逻辑”改为“禁止臆造选择逻辑”
  3. `OPTIMIZER_SYSTEM_CN/EN` 咖啡示例改为“优先当前位置或默认地址，不臆造新地址”
- **修改文件**：`PromptRegistry.kt`
- **状态**：✅ 已完成

### 需求5：地址页优先就近选择 + 输入法任务级切换
- **来源**：用户反馈进入地址页面后会反复打开搜索且不知道搜什么；并且 ADB Keyboard 来回切换导致输入不稳定
- **修复**：
  1. `AGENT_SYSTEM_CN/EN` 新增“地址页操作顺序”规则：已有当前位置/附近地址列表时优先就近选择，不立刻搜索
  2. `ActionExecutor.kt` 将美团/美团外卖加入剪贴板优先输入包名，且剪贴板输入前先尝试全选，减少追加输入
  3. `ShellExecutor.kt` 新增任务级 IME 会话：`beginAdbKeyboardSession()` / `endAdbKeyboardSession()`，保存并恢复原输入法
  4. `PhoneAgent.kt` 在 `run()` 与 `resumeWithIntervention()` 生命周期接入 IME 会话：任务开始切到 ADB Keyboard，任务结束/打断恢复原输入法
- **修改文件**：`PromptRegistry.kt`、`ActionExecutor.kt`、`ShellExecutor.kt`、`PhoneAgent.kt`
- **状态**：✅ 已完成

---

## 2026-04-05 会话（第二轮及以后）

### 问题修复8：系统通知栏不可见（MIUI + 通道缓存）
- **现象**：后台运行时系统下拉通知栏中无任何通知，用户无法感知服务运行状态
- **根因**：
  1. 通知通道使用 `IMPORTANCE_LOW`，MIUI 默认隐藏低优先级通知
  2. Android 缓存通知通道设置：代码修改 importance 后，已存在的同 ID 通道**不会更新**
- **修复**：
  1. `App.kt`：通道 ID 改为 `autoglm_service_channel_v2`，先删除旧通道再创建新通道
  2. `App.kt`：`IMPORTANCE_LOW` → `IMPORTANCE_DEFAULT`
  3. `MainActivity.kt`：`executeTask()` 新增 `POST_NOTIFICATIONS` 运行时权限请求（Android 13+）
- **修改文件**：`App.kt`、`MainActivity.kt`
- **状态**：✅ 已修复

### 问题修复9：IME 输入法不还原（协程取消 + 时序 + 服务销毁）
- **现象**：退出 AutoGLM 或任务取消后，输入法停留在 ADB Keyboard
- **根因**：`endAdbKeyboardSession()` 是 suspend 函数，在已取消协程的 `finally` 块中调用会抛 `CancellationException` 导致还原失败。总结生成前输入法未提前释放等。
- **修复**：
  1. `PhoneAgent.kt`：`run()` / `intervention()` 的 `finally` 中 `endAdbKeyboardSession()` 用 `withContext(NonCancellable)` 包装。
  2. 在总结生成前及各中止节点注入提前恢复 IME 的调用。
  3. `WakeWordService.kt`：`onDestroy()` 添加输入法恢复保障。
- **修改文件**：`PhoneAgent.kt`、`WakeWordService.kt`
- **状态**：✅ 已修复

### 问题修复10：Porcupine 唤醒词语言模型不匹配
- **现象**：初始化报错 Keyword file (zh) 与 model file 不匹配
- **根因**：SDK 默认加载英文 `.pv` 模型
- **修复**：添加 `porcupine_params_zh.pv` 并通过 `needChineseModel` 判断后调用 `setModelPath()`
- **修改文件**：`PorcupineWakeEngine.kt`
- **状态**：✅ 已修复

### 需求6：语音唤醒服务自动启动及UI逻辑优化
- **现象**：每次打开应用需手动启动服务，且带有一个误导的屏幕分享图标。
- **修复**：
  1. `MainActivity.kt` 新增 `autoStartServiceIfEnabled()`，如果 `wakeWordEnabled` 开关开启且具备权限，则自动自启。
  2. 去除无用的投屏/分享桌面 IconButton。
- **修改文件**：`MainActivity.kt`
- **状态**：✅ 已完成

### 问题修复11：Compose 状态不响应服务绑定（UI不刷新）
- **现象**：服务后台已经连上并在运行，首页状态仍显示“语音唤醒未启动”且不刷新状态
- **根因**：`wakeWordService` 非 Compose 状态变量，服务绑定结果无法被 UI 观察更新。
- **修复**：增加 `_serviceConnected` 的 `mutableStateOf(false)`，连接后改变该值触发 `setContent` 的 recomposition 刷新 `MainScreen` 的入参。
- **修改文件**：`MainActivity.kt`
- **状态**：✅ 已修复

### 问题修复12：前台服务自启崩溃退出（Android 14 MediaProjection 权限）
- **现象**：加入自启逻辑后，应用打开时仍然显示为"语音唤醒未启动"。
- **根因**：（这个是被隐藏的雷）Android 14 要求带 `mediaProjection` 类型的前台服务必须在有相应的权限 Token 的上下文中被启动；直接后台开启会导致系统直接抛出 `SecurityException`，并且被通用捕获而立即调用 `stopSelf()` 默默销毁服务。
- **发现过程**：adb 日志只过滤 `AutoGLM`，而该类异常直接抛在了 `WakeWordService` 默认的 TAG 下，导致日志被隐藏。
- **修复**：修改了自启配置（在 `App.NOTIFICATION_ID` 中不在静默启动阶段强制挂载 `ServiceInfo.FOREGROUND_SERVICE_TYPE_MEDIA_PROJECTION`），移除该限制仅获取 microphone 和 task 处理的服务类型，随后成功在后台站住并且进入监听状态。
- **修改文件**：`WakeWordService.kt`
- **状态**：✅ 已修复

### 问题修复13：应用内启动失败（不需要悬浮窗）
- **现象**：在应用内明明已经启动了，但还是会要求给出悬浮窗权限，影响单纯在App内使用唤醒的体验。
- **原因**：`MainActivity` 强制校验了 `Settings.canDrawOverlays`。
- **解决方案**：将强制校验改为发 Warning 记录。用户可以直接在App内点击按钮启动唤醒，此时 `WakeWordService` 正常启动并在后续触发时显示半透明助手窗体，仅在外层展现时才存在一些限制，整体不再强阻断使用流程。

### 问题修复14：唤醒词被检测到后就闪退 / "权限不足"错误
- **现象**：当从后台状态唤醒时，STT报 "权限不足" 错误并立刻退出监听。
- **原因**：Android 11 以上系统禁止后台 Service 隐式启动自带的麦克风或 `SpeechRecognizer`。麦克风权限只允许在 UI Activity / Foreground 模式下发起。
- **解决方案**：修改了架构。服务在监听到唤醒词后，会使用FLAG_ACTIVITY_NEW_TASK呼出一个半透明无窗口样式的Activity：`AssistantActivity`。由这个Activity充当前台UI主体回调通知 Service 此时再进行 STT 启动。绕过了Android系统的隐式后台麦克风保护机制。

---

## 2026-04-06 会话

### 问题修复15：IME 恢复时 originalIme 为空
- **现象**：任务结束后输入法不恢复，日志 `[IME_SESSION] 已开启，originalIme=`（空字符串）
- **根因**：`beginAdbKeyboardSession()` 使用 `execute("settings get secure default_input_method", useRoot=true)` 读取当前输入法，但非 root 设备 shell 命令返回空
- **修复**：改用 Java API `Settings.Secure.getString(contentResolver, "default_input_method")`，shell 命令作为兜底
- **修改文件**：`ShellExecutor.kt`
- **状态**：✅ 已修复

### 问题修复16：输入框显示"识别中: xxx"前缀
- **现象**：IME 语音识别时，WakeListeningOverlay 输入框内显示 `"识别中: 你好"` 而不是纯文字 `"你好"`
- **根因**：`onStatusChange` 直接把含前缀的状态字符串调 `setInputFieldText(status)` 写入输入框
- **修复**：识别文字（`"识别中: xxx"`）只将纯文字部分写入输入框；"等待键盘就绪..."/"请说话..."等非识别状态通过 `updatePartialResult()` 显示在蓝色文字区
- **修改文件**：`WakeWordService.kt` onStatusChange 回调
- **状态**：✅ 已修复

### 问题修复17：启动时误报"语音识别不可用" Toast
- **现象**：App 启动后弹出 Toast "语音识别失败: 语音识别不可用"，但实际使用 Porcupine + IME_VOICE 模式正常
- **根因**：`initializeComponents()` 无条件初始化系统 `SpeechRecognizer`，在无 GMS 设备上 `isRecognitionAvailable()` 返回 false 触发误导性错误
- **修复**：仅当 `commandSttMode == "SYSTEM"` 时才初始化系统语音识别器
- **修改文件**：`WakeWordService.kt` initializeComponents()
- **状态**：✅ 已修复

### 问题修复18：停止任务后语音唤醒变成"未启动"
- **现象**：用户点击悬浮条停止按钮后，左上角图标变为"语音唤醒未启动"，需手动重启服务
- **根因**：`stopCurrentTask()` 将状态设为 `IDLE` 但未调用 `startWakeWordListening()` 恢复唤醒监听
- **修复**：在 `stopCurrentTask()` 末尾加 `startWakeWordListening()` 自动恢复
- **修改文件**：`WakeWordService.kt`
- **状态**：✅ 已修复

### 问题修复19：任务总结阻塞服务恢复（17-20秒）
- **现象**：任务完成后唤醒监听需等待 17-20 秒才恢复（期间进程可能被系统杀死导致完全丢失）
- **根因**：PhoneAgent 中 `generateTaskSummaryIfEnabled()` 调用 LLM 生成总结耗时 17-20 秒，期间 `onTaskComplete` 回调被阻塞，服务无法恢复唤醒
- **修复**：将 `onTaskComplete` 调用提前到总结生成之前，先用任务结果（如 `decision.assessment` 或 `result.message`）立即通知服务恢复，总结异步生成后通过 `onTaskSummary` 回调更新
- **修改文件**：`PhoneAgent.kt` 的 `executeDirectly()` 和 `runWithCoordinator()`
- **状态**：✅ 已修复

### 问题修复20：ADB_INPUT_TEXT 广播权限不足（am broadcast → Java API）
- **现象**：非 root 设备执行 `am broadcast -a ADB_INPUT_TEXT` 时报 `INTERACT_ACROSS_USERS` 权限错误，文本输入失败
- **根因**：App 进程通过 shell 执行 `am broadcast` 发送到另一个进程的广播时，系统要求跨用户权限
- **修复**：新增 `sendAdbInputBroadcast()` 方法，优先使用 Java API `context.sendBroadcast(Intent("ADB_INPUT_TEXT"))` 直接发广播（不需要跨用户权限）；无 context 时降级到 shell 命令并指定 `--user 0`
- **修改文件**：`ShellExecutor.kt`
- **状态**：✅ 已修复

### 问题修复21：语音按钮从长按空格改为无障碍单击
- **现象**：IME 语音输入模式下，原方案通过 `su input keyevent --longpress KEYCODE_SPACE` 触发语音输入，非 root 设备无法使用
- **根因**：长按空格键需要 root 权限执行 shell 命令
- **修复**：改用 `UIHierarchyManager.performClick(context, x, y)` 通过无障碍服务执行单击操作；新增 `tapVoiceButton()` 方法在 IO 线程异步执行点击
- **修改文件**：`ImeVoiceSttHelper.kt`
- **状态**：✅ 已修复

### 问题修复22：PPN 文件导入不持久化
- **现象**：导入自定义唤醒词 ppn 模型文件后，重启应用丢失配置
- **根因**：ppn 文件导入后仅更新了 UI 状态变量，未写入 SharedPreferences
- **修复**：导入成功后立即写入 `prefs.customPpnName`、`prefs.customPpnPath`、`prefs.wakeWordKeyword = "CUSTOM"`，不经过通用的 saveSettings 弹窗流程
- **修改文件**：`MainActivity.kt`
- **状态**：✅ 已修复

### 问题修复23：设置页返回可能丢失修改
- **现象**：用户修改设置后按返回键，直接退出设置页面，修改内容丢失
- **修复**：`BackHandler` 始终拦截返回事件并弹出确认对话框，根据 `hasChanges` 显示不同提示（"以下设置已修改" / "是否保存设置？"），用户可选择保存/不保存/取消
- **修改文件**：`MainActivity.kt`
- **状态**：✅ 已修复

### 问题修复25：语音唤醒任务不计入对话历史
- **现象**：语音唤醒触发的任务执行完后，对话列表里找不到任何记录
- **根因**：`LaunchedEffect(lastRecognizedText?.value)` 里有 `currentRoute == "chat"` 条件，用户在 Home 页面时语音唤醒，判断不满足，跳过了 `addMessage` 和 `taskOwnerConversationId`；协调器/执行器消息也有 `currentRoute != "chat"` 的提前返回，导致全部消息被丢弃
- **修复**：
  1. `lastRecognizedText` LaunchedEffect：去掉 `currentRoute == "chat"` 判断；若无当前对话则自动 `createNewConversation()`；任务锁定 `taskOwnerConversationId` 后，若不在 chat 页面则调用 `navController.navigate("chat")` 自动跳转
  2. coordinator/agent LaunchedEffect：删除 `if (currentRoute != "chat") return@LaunchedEffect`，依赖 `addMessageToTask`/`updateMessageInTask` 自动路由到任务所属对话
- **修改文件**：`MainActivity.kt`
- **状态**：✅ 已修复

### 需求7：README 重写 + 设计文档
- **内容**：README 改写为面向用户的安装使用文档，重点补充 Porcupine Access Key 申请步骤、自定义唤醒词训练流程、快捷指令（Skill）+ 意图识别路由说明；新增技术方案文档说明整体架构与事件机制
- **新增文档**：`docs/技术方案/20260406_系统运作流程与事件机制.md`
- **状态**：✅ 已完成
- **现象**：在一个对话里发起任务后切换到新对话，老任务的执行消息出现在新对话里
- **根因**：`addMessage()` 始终写入 `currentConversation`，切换对话后 `currentConversation` 指向新对话
- **修复**：新增 `taskOwnerConversationId` 状态变量，在任务发起时锁定（语音识别/快捷指令/手动输入三处）；新增 `addMessageToTask()`/`updateMessageInTask()` 辅助函数路由消息；coordinator 和 agent 所有消息写入改用路由函数；RESULT 消息后清除锁定
- **修改文件**：`MainActivity.kt`
- **状态**：✅ 已修复

### 需求7：任务总结悬浮窗（TaskSummaryOverlay）
- **来源**：用户希望任务完成后以可关闭的浮窗展示 Markdown 格式的任务总结，而非仅在聊天中显示
- **实现**：
  - 新建 `TaskSummaryOverlay.kt`：半透明遮罩 + 居中圆角卡片（88%屏宽、65%最大高度）
  - 标题栏："任务总结" + 关闭按钮（X符号）
  - 内容区：ScrollView + TextView，通过 `Html.fromHtml()` 渲染 Markdown 转 HTML
  - 底部：居中"关闭"按钮
  - Markdown 转换支持：**粗体**、*斜体*、`代码`、# 标题（绿色）、- 列表项
  - 深色主题配色与 InterventionInputOverlay 风格一致
  - `WakeWordService.kt` 集成：`onTaskSummary` 回调触发显示；前/后截图隐藏恢复；新任务开始时/停止时自动关闭；onDestroy 清理
- **修改文件**：`TaskSummaryOverlay.kt`（新建）、`WakeWordService.kt`
- **状态**：✅ 已完成

### 问题修复27：多设备运行时 Porcupine/STT 均初始化失败、错误提示不友好
- **现象**（另一台手机）：
  1. `Porcupine 初始化错误: /data/...你好小爱_zh_android_v4_0_0.ppn`（ppn 文件当时不存在于设备）
  2. 导入 ppn 后仍出现 `Porcupine 初始化错误: Initialization failed: [0] d3ff828 00000136...`（Native runtime 错误，极可能是同一 AccessKey 在多台设备上被激活超限）
  3. 系统 STT 也失败：`SpeechRecognizer.isRecognitionAvailable()` 返回 false（无 GMS 的国产 ROM）
  4. 两个引擎均失败后，`_serviceState = IDLE`，UI 显示停止按钮消失，用户无法感知原因
- **根因**：
  - Porcupine：同一 AccessKey 激活设备超限；原代码对所有 `PorcupineException` 子类一视同仁，错误消息只有 native stacktrace
  - 系统 STT：国产 ROM 无 Google 语音服务；错误描述不够明确
  - 双重失败：`_lastWakeWordError` 有值但仅更新 StateFlow，未主动 Toast 提示用户
  - 停止按钮消失：`isServiceRunning = serviceState != IDLE`，引擎失败后 serviceState 被设为 IDLE，导致服务明明在跑但 UI 认为"未启动"，停止按钮不出现
- **修复**：
  - **P1** `PorcupineWakeEngine.kt`：细化 catch 分支，区分 `ActivationRefusedException`/`ActivationLimitException`/`ActivationThrottledException`/`RuntimeException`/`IOException`，各给可读错误消息
  - **P2** `SystemSttWakeEngine.kt`：`checkRecognitionSupport()` 错误消息明确说明国产 ROM 场景及"改用 API 模式"建议
  - **P0** `WakeWordService.kt`：均失败时用 `withContext(Main)` 弹出包含双引擎错误原因和操作建议的 Toast
  - **P3** `WakeWordService.kt`：新增 `wakeListeningJob` 引用，`startWakeWordListening()` 入口检测 `isActive` 避免并发重入（invoke + onStartCommand 同时触发时只初始化一次）
  - **停止按钮** `MainActivity.kt`：`isServiceRunning = serviceState != null`，只要服务已绑定即显示停止按钮，不受 IDLE 状态影响
- **修改文件**：`PorcupineWakeEngine.kt`、`SystemSttWakeEngine.kt`、`WakeWordService.kt`、`MainActivity.kt`
- **状态**：✅ 已修复

### 需求8：ADB Keyboard 未安装提示改为悬浮窗卡片（含可复制下载链接）
- **背景**：原实现在 ADB Keyboard 输入失败时弹出系统 Toast + 自动打开浏览器，链接无法复制，体验较差
- **方案**：新建 `AdbKeyboardOverlay.kt`，以悬浮窗卡片形式提示，参考 `TaskSummaryOverlay` 风格；通过 callback 链路传递：`ActionExecutor` → `PhoneAgent` → `WakeWordService`
- **实现**：
  - `AdbKeyboardOverlay.kt`（新建）：深色圆角卡片，标题 + 说明文字 + 可复制链接行（复制按钮 + "打开下载页"按钮）+ 关闭按钮；`FLAG_NOT_FOCUSABLE` 保持输入焦点
  - `ActionExecutor.kt`：新增 `var onAdbKeyboardNotInstalled: (() -> Unit)?`，将原 Toast + 浏览器调用替换为 `onAdbKeyboardNotInstalled?.invoke()`；删除 `showAdbKeyboardDownloadToast()` 方法；移除 Handler/Looper/Toast import
  - `PhoneAgent.kt`：新增 `var onAdbKeyboardNotInstalled: (() -> Unit)?`，带自定义 setter（初始化后同步到 actionExecutor）；`initialize()` 中将当前值同步到新创建的 actionExecutor
  - `WakeWordService.kt`：新增 `adbKeyboardOverlay` 字段，`initializePhoneAgent()` apply 块中注册 `onAdbKeyboardNotInstalled = { showAdbKeyboardOverlay() }`，新增 `showAdbKeyboardOverlay()` 方法，`onDestroy` 中 dismiss + null 清理
- **修改文件**：`AdbKeyboardOverlay.kt`（新建）、`ActionExecutor.kt`、`PhoneAgent.kt`、`WakeWordService.kt`
- **状态**：✅ 已完成

## 2026-04-06 会话

### 问题修复1：语音唤醒悬浮框退格无法清除第一个字

- **现象**：语音识别结果回填到 `WakeListeningOverlay.inputField` 后，用户退格逐字删除，最后一个字（即第一个字）无法被清除
- **根因**：`ImeVoiceSttHelper` 使用内部隐藏 `editText` 持有 IME 焦点，软键盘退格实际作用于此 editText，`inputField` 通过 `setInputFieldText` 被动同步。当 editText 退格清空时，`afterTextChanged` 里 `if (text.isBlank()) return` 阻止了向外通知，导致 `inputField` 残留最后一个字。
- **修复**：`ImeVoiceSttHelper.kt`：`text.isBlank()` 时发送 `onStatusChange?.invoke("识别中: ")`（空文字通知），触发 `setInputFieldText("")` 清空 `inputField`，再 return
- **修改文件**：`ImeVoiceSttHelper.kt`
- **状态**：✅ 已修复

### 问题修复2：语音唤醒将消息写入旧的历史对话

- **现象**：语音唤醒识别结果被追加到之前已有的历史对话中，而非新建对话
- **根因**：`LaunchedEffect(lastRecognizedText?.value)` 中创建条件为 `if (currentConversation == null) createNewConversation()`，有旧对话时直接复用而不新建
- **修复**：`MainActivity.kt`：改为每次语音唤醒无条件调用 `createNewConversation()`，始终开启新对话
- **修改文件**：`MainActivity.kt`
- **状态**：✅ 已修复

### 需求1：快捷指令支持单独配置指令优化器

- **背景**：快捷指令已支持独立配置"启用规划"，但"启用优化器"开关始终硬编码为 `true`，无法按指令独立控制
- **实现**：
  - `ShortcutManager.kt`：`ShortcutData` 添加 `enableOptimizer: Boolean = true` 字段（向后兼容，旧数据 Gson 反序列化默认 `true`）
  - `ShortcutEditDialog.kt`：新增"启用指令优化"开关，紧随"启用规划"开关之下；`hasUnsavedChanges` 和保存逻辑同步加入 `enableOptimizer`
  - `HomeScreen.kt`：无参数快捷指令直接点击时，将 `enableOptimizer` 从硬编码 `true` 改为 `shortcut.enableOptimizer`
  - `ParameterInputDialog.kt`：有参数快捷指令弹窗的 `enableOptimizer` 初始值从硬编码 `true` 改为 `shortcut.enableOptimizer`
- **修改文件**：`ShortcutManager.kt`、`ShortcutEditDialog.kt`、`HomeScreen.kt`、`ParameterInputDialog.kt`
- **状态**：✅ 已完成

### 需求2：快捷指令支持导出/导入（与设置页面一致）

- **背景**：设置页面已有配置导出/导入功能，快捷指令同样存储为 JSON，用户希望能够在设备间迁移或备份快捷指令
- **实现**：
  - `HomeScreen.kt`：
    1. 新增 imports：`rememberLauncherForActivityResult`、`ActivityResultContracts`、`Gson`、`TypeToken`、`sp`
    2. 注册 `exportShortcutsLauncher`（`CreateDocument("application/json")`）：导出序列化 `shortcuts` 并写入用户选定文件，成功提示"已导出 N 条快捷指令"
    3. 注册 `importShortcutsLauncher`（`GetContent()`）：读取 JSON → `TypeToken<List<ShortcutData>>` 反序列化 → `saveShortcuts()` → 刷新 `shortcuts` 状态，成功提示"已导入 N 条快捷指令"
    4. "快速操作"标题行右侧新增"导入"（FolderOpen 图标）+"导出"（Share 图标）TextButton，与设置页风格一致；原"完成"/"长按编辑排序"保持不变，嵌入同一 Row
  - 导出文件命名格式：`autoglm_shortcuts_yyyyMMdd_HHmmss.json`
  - 导入采用**合并策略**：id 相同则替换，id 不存在则追加，本地独有快捷指令保留；提示"导入完成：更新 N 条，新增 N 条"
- **修改文件**：`HomeScreen.kt`
- **状态**：✅ 已完成

### 问题修复28：语音唤醒后优化指令气泡重复弹出（双 bubble）

- **现象**：语音唤醒触发任务后，聊天页出现两个优化指令气泡（同时流式更新）
- **根因**：
  - `handleSpeechResult` 中 `_lastRecognizedText.value = text` 与 `phoneAgent.run()` 几乎同时触发
  - 前者通过 `LaunchedEffect(lastRecognizedText)` → `createNewConversation()` → 对话 ID 改变 → `LaunchedEffect(currentConversation?.id)` 将 `optimizerMessageIndex` 重置为 -1
  - 此时 optimizer 已发出第一个 `OPTIMIZER_STREAMING` token 并建立了 bubble #1（`optimizerMessageIndex = 0`）
  - 索引被重置后，下一个 `OPTIMIZER_STREAMING` 再次走"新建"分支，建立 bubble #2
  - 两个 bubble 同时更新
- **修复**：`MainActivity.kt` — `LaunchedEffect(currentConversation?.id)` 不再重置 `optimizerMessageIndex`、`plannerMessageIndex`、`summaryMessageIndex`、`stepMessageIndex`；这些索引改为只由 `CoordinatorMessageType.CLEAR` 消息重置（任务完成/出错时发送，已有实现）
- **修改文件**：`MainActivity.kt`
- **状态**：✅ 已修复

### 需求3：IME 语音键盘就绪延迟可配置

- **背景**：`ImeVoiceSttHelper` 弹出输入法后等待键盘就绪的延迟原为硬编码 800ms，部分设备键盘弹出较慢，导致点击语音按钮时键盘尚未就绪（点击无效）
- **实现**：
  - `PreferenceManager.kt`：新增 `KEY_IME_VOICE_KEYBOARD_DELAY`、`DEFAULT_IME_VOICE_KEYBOARD_DELAY_MS = 1200L`（上调默认值）以及 `imeVoiceKeyboardDelay: Long` 属性
  - `ImeVoiceSttHelper.kt`：`KEYBOARD_READY_DELAY_MS` 常量重命名为 `KEYBOARD_READY_DELAY_DEFAULT_MS`（兜底值），新增实例变量 `keyboardReadyDelay`，`start()` 改用 `keyboardReadyDelay` 控制延迟
  - `WakeWordService.kt`：初始化 `ImeVoiceSttHelper` 时注入 `prefs.imeVoiceKeyboardDelay`
  - `MainActivity.kt`：设置页面在坐标 X/Y 下方新增"键盘就绪延迟（毫秒）"输入框；同步加入 original/save/changedItems 逻辑
- **修改文件**：`PreferenceManager.kt`、`ImeVoiceSttHelper.kt`、`WakeWordService.kt`、`MainActivity.kt`
- **状态**：✅ 已完成

### 问题修复29：IME 语音点击不遵循 Root 模式设置

- **现象**：开启了"启用Root"设置后，IME 语音的语音按钮点击仍不生效（点击实际走了无障碍服务路径，Root 被忽略）
- **根因**：`ImeVoiceSttHelper.tapVoiceButton()` 只调用 `UIHierarchyManager.performClick()`，注释写"不需要 root"，完全没有参考 `ShellExecutor.globalUseRoot` 状态，与 `ActionExecutor.getEffectiveMode()` 的 root 优先逻辑不一致
- **修复**：`tapVoiceButton()` 改为与 `ActionExecutor.getEffectiveMode()` 保持一致的判断顺序：
  1. `ShellExecutor.globalUseRoot == true` → `ShellExecutor.tap(x, y)`（root `input tap`）
  2. 否则 → `UIHierarchyManager.performClick()`（无障碍服务手势）
- **修改文件**：`ImeVoiceSttHelper.kt`
- **状态**：✅ 已完成
